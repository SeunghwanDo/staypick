# StayPick (스테이픽)
**체류시간 기반 트렌드 콘텐츠 자동 생성기**

**한 줄:** Analytics에서 **방문수 + 체류시간** 상위 콘텐츠를 뽑아 **요약/인사이트**화하고, 타겟 페르소나별 **새 콘텐츠(뉴스레터/블로그/스레드/숏폼 대본 등)** 를 자동 생성합니다.

- 버전: v0.1
- 기준일: 2026-02-14
- 제출자: (이름/팀)
- 연락처: (이메일)

---

## 1) 문제(Problem)
- 콘텐츠 팀은 무엇이 ‘먹히는지’ 찾기 위해 리서치/요약/초안 작성에 많은 시간을 씁니다.
- 단순 조회수는 ‘진짜 관심’을 반영하지 못해, 트렌드 판단이 흔들립니다.
- 채널별(뉴스레터/블로그/스레드/숏폼)로 재가공하는 과정이 반복 노동입니다.

## 2) 해결(Solution)
- StayPick은 **체류시간(dwell)** 기반으로 상위 콘텐츠를 자동 선정하고, 요약/인사이트를 **구조화(JSON)** 합니다.
- 타겟 페르소나와 포맷을 선택하면, ‘완전히 새로운’ 콘텐츠 초안을 즉시 생성합니다.
- 출처(Sources)와 리스크 노트를 포함해, 빠르고 안전하게 발행할 수 있습니다.

## 3) 핵심 기능
- Top 콘텐츠 랭킹: visits + avg dwell 가중치 스코어
- URL 본문 수집/텍스트 추출(실패 시 자동 스킵)
- 요약 결과를 JSON Schema로 고정: one-liner, key points, tags, angles, risk notes
- 요약 임베딩 기반 토픽 클러스터(트렌드 묶음) (선택)
- 포맷별 콘텐츠 생성: 뉴스레터/블로그/스레드/숏폼 대본/슬라이드 스크립트
- Markdown 다운로드(복사/편집/발행 용이)

## 4) OpenAI API 사용(요건 충족)
- **Responses API**: 요약 + 새 콘텐츠 생성
- **Structured Outputs(JSON Schema)**: 요약 결과를 안정적으로 구조화
- **Embeddings API**: 요약 기반 트렌드 클러스터링(주제 묶음)
- (옵션) **web_search tool**: 외부 트렌드 참고

## 5) 데모 시나리오(60초)
1) CSV 업로드 → Top N 자동 선정  
2) ‘요약 생성’ 클릭 → URL 본문 수집 + 요약/인사이트 생성  
3) 클러스터(트렌드 묶음) 3개 확인  
4) 페르소나/포맷 선택 → ‘새 콘텐츠 생성’  
5) Markdown 다운로드 → 바로 발행/편집  

## 6) 비즈니스 가치
- 제작 리드타임 단축: 리서치/요약/초안 자동화
- 데이터 기반 기획: ‘오래 읽힌’ 관심사를 중심으로 주제 선정
- 멀티 채널 확장: 동일 인사이트를 채널별 포맷으로 자동 변환
- 내부 자산 재활용: 과거 히트 콘텐츠를 새로운 각도로 재생산

## 7) 실행 방법(MVP)
```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt

export OPENAI_API_KEY="YOUR_KEY"  # Windows PowerShell: $env:OPENAI_API_KEY="YOUR_KEY"
streamlit run app.py
```

> 모델이 안 되면 UI에서 `gpt-4o-mini` 등 접근 가능한 모델로 변경하세요.

---

## 주의 사항
- URL 크롤링은 사이트 약관/robots.txt/저작권 정책을 준수해야 합니다. 내부 문서 또는 공개 허용된 자료를 대상으로 사용하세요.
- API Key는 환경변수로만 보관하는 것을 권장합니다.
